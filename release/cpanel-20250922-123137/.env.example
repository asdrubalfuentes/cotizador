# --- Server ---
# PORT es opcional en cPanel (Passenger lo gestiona). Úsalo sólo en local.
PORT=3000

# Directorio donde se generan/leen PDFs, JSON, QR y logos.
# Por defecto es ./outputs dentro del proyecto. Puedes apuntar a otro path con permisos de escritura.
# OUTPUTS_DIR=/home/usuario/cotizador/outputs

# Logging HTTP (morgan). Ej.: 'dev', 'combined'
MORGAN_FORMAT=dev

# --- Seguridad / Auth ---
JWT_SECRET=pon-aqui-una-clave-segura-y-larga
ADMIN_PASSWORD=cambia-esto-para-el-login-admin

# --- Frontend ---
# URL pública donde se sirve la app (se usa en PDF/QR y correos)
FRONTEND_URL=https://cotizador.tudominio.cl

# --- SMTP / Correo ---
SMTP_HOST=smtp.tudominio.cl
SMTP_PORT=587
# true para 465 (SSL/TLS), false para 587 (STARTTLS)
SMTP_SECURE=false
SMTP_USER=usuario@tudominio.cl
SMTP_PASS=tu_password
# Dirección FROM. Si no se define, se usa SMTP_USER.
SMTP_FROM="Cotizador <no-reply@tudominio.cl>"

# Opcionales (timeouts y depuración)
SMTP_CONN_TIMEOUT=15000
SMTP_GREET_TIMEOUT=10000
SMTP_SOCKET_TIMEOUT=20000
SMTP_TLS_REJECT_UNAUTH=true
SMTP_DEBUG=false

# Destinatarios auxiliares (opcional)
# Si se define, forzará el envío de copias de pruebas a esta dirección.
SMTP_FALLBACK_TO=
# Notificaciones internas de cambios de estado
SMTP_NOTIFY_TO=
# Copia este archivo a .env y ajusta los valores
SMTP_HOST=smtp.example.com
SMTP_PORT=465
# SMTP_SECURE=true para SMTPS (465). Para STARTTLS en 587 usa false.
SMTP_SECURE=true
SMTP_USER=account@example.com
SMTP_PASS=change-me
# Remitente visible en el correo; si se omite usar e1 SMTP_USER
SMTP_FROM="Cotizador <account@example.com>"
# Opcionales: timeouts (ms) y TLS relajado si el servidor usa cert no v e1lido
# SMTP_CONN_TIMEOUT=15000
# SMTP_GREET_TIMEOUT=10000
# SMTP_SOCKET_TIMEOUT=20000
# SMTP_TLS_REJECT_UNAUTH=false
# SMTP_DEBUG=true
# SMTP_TEST_TO=otro@ejemplo.com
JWT_SECRET=please-change-me
# Puertos/Protocolos
# Si configuras HTTPS=true o colocas los certificados en /etc/ssl/emqx, el server levantará HTTPS.
# HTTPS_PORT por defecto 8443; PORT se usa para HTTP (fallback o cuando no hay certificados)
PORT=5000
HTTPS=false
HTTPS_ENABLE=false
HTTPS_PORT=8443
TLS_CERT_FILE=/etc/ssl/emqx/emqx.crt
TLS_KEY_FILE=/etc/ssl/emqx/emqx_key.rsa
TLS_CA_FILE=/etc/ssl/emqx/emqx.ca-bundle.crt
FRONTEND_URL=http://localhost:5173
# En producción, establece FRONTEND_URL a la URL pública del sitio, por ejemplo:
# FRONTEND_URL=https://cotizador.tudominio.cl

# --- Ejemplo cPanel/Hosting ---
# SMTP_HOST=mail.aysafi.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=contacto@aysafi.com
# SMTP_PASS=<tu-contraseña>
# SMTP_FROM="Cotizador AYSAFI <contacto@aysafi.com>"


######    El que ya esta subido	   ########
# URL públicas
FRONTEND_URL=https://cotizador.aysafi.com
PUBLIC_API_BASE=https://emqx.aysafi.com
PORT=5000
HTTPS=true
HTTPS_ENABLE=true
HTTPS_PORT=8443
TLS_CERT_FILE=/etc/ssl/emqx/emqx.crt
TLS_KEY_FILE=/etc/ssl/emqx/emqx_key.rsa
TLS_CA_FILE=/etc/ssl/emqx/emqx.ca-bundle.crt

OUTPUTS_DIR="outputs/"

# SMTP for email notifications
SMTP_HOST="mail.aysafi.com"
SMTP_PORT=465
SMTP_USER="contacto@aysafi.com"
SMTP_PASS="s]qYXOyD{PRn"
SMTP_SECURE=true
SMTP_FROM="<Cotizador AYSAFI>contacto@aysafi.com"
#FRONTEND_URL=https://cotizador.aysafi.com
SMTP_CONN_TIMEOUT=30000
SMTP_GREET_TIMEOUT=20000
SMTP_SOCKET_TIMEOUT=40000
DIAGNOSE_SEND=true
SMTP_TLS_REJECT_UNAUTH=false
SMTP_DEBUG=false
# Optional: additional recipient for notifications (BCC)
SMTP_NOTIFY_TO="administracion@aysafi.com"

# Request logging format (morgan)
MORGAN_FORMAT="dev"

# Admin authentication
ADMIN_PASSWORD="s]qYXOyD{PRn"
JWT_SECRET="s]qYXOyD{PRn"

#frontend data
FRONTEND_URL="https://cotizador.aysafi.com"


